<h1>Profile</h1>
  <ul>
    <li>Username: @<%= current_account.username %></li>
    <li>Name: <%= current_account.name %></li>
  </ul>
<h2>Tweets</h2>
<ul>
  <% @tweets.reverse.select do |tweet| %>
    <% if tweet.account_id == current_account.id %>
      <li>
        <%= current_account.name %>
        @<%= current_account.username %>
        <%= tweet.content  %>
        <%= tweet.created_at %>
      </li>
    <% end %>
  <% end %>
</ul>
